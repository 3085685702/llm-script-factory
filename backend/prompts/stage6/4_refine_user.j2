【当前任务】
你现在的任务是【治疗/润色】。根据指令，修改当前剧本。

【上一集情节点（仅供参考！）】
{{ prev_script_summary }}

【下一集情节点（仅供参考！）】
{{ next_script_summary }}

【本集原剧本】
{{ current_script }}

【重要：当前指令】
请按照以下要求修改当前剧本：
{{ instruction }}

{% if custom_instruction %}
【用户详细指令（最高优先级）】
{{ custom_instruction }}
{% endif %}

### 输出格式
请遵循【修改指令】的修改要求，结合你对短剧、DTG理论的深度理解，严格以 JSON 格式输出，不要包含 Markdown 代码块标记（如 ```json）。你需要将精修后的内容按“场次”拆解。

注意：
1. **严格保留**：原剧本的场号、时间、人物、地点等信息，这4部分直接照抄。
2. 正文括号里的表情、语气，应当换成2-4字的（2字为佳）简单明确的表情、神态，就如同修改范例的修改过程一样（你被允许这么做）。
3. 频繁出现问题（重视）：对于▲后的动作、镜头，如果是同一人的戏份，尽量写为一行，融合为一行，如非必要，不要拆分多行（你被允许这么做）。
4. ▲后的动作、镜头应当与台词分行撰写，不可写在同一行。
5. ▲后的动作、镜头应当简洁、明确、直白易懂、易于拍摄、易于演员理解，故禁止使用形容词，禁止抽象表达。
6. OS定义：OS一般用于角色内心思考、想法、独白等。
7. VO定义：VO一般指画外音，比如某个角色从电话里发出的声音，那就是：某某VO：...。
8. **配角反应**：主角的装逼打脸、超常行为，必须有配角的精彩反应，这是爽点的重要来源。
9. **剧情连贯性**：注意前后剧集的剧情连贯性、逻辑通顺性、合理性，不可出现剧情突兀、不连贯、甚至断档的情况。
10. **戏份要求**：至少满足10人次戏份（这里仅指台词戏份，不同角色，包括但不限于台词、OS、VO，但不包括任何▲内容）。
11. 采用简体中文。

格式如下：
{
    "episodes": [
        {
            "ep_id": <int: 当前集数>,
            "scenes": [
                {
                    "scene_id": "<string>",
                    "time": "<string>",
                    "location": "<string>",
                    "characters": "<string>",
                    "content": "<string: 精修正文>"
                }
            ]
        }
    ]
}
